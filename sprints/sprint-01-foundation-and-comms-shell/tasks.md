# Sprint 01 — Tasks

> Before implementing any Sprint 01 work, change the matching task row or rows to `In Progress`. If one delivery bundle covers several tasks, update all included rows together before making edits.

## Autonomous Parent Stream

`S01-AUTO` is the parent delivery stream for the 18 Sprint 01 tasks that can be executed without human-eye QC. These task rows are treated as ordered subtasks:

1. `S01-T01`
2. `S01-T03`
3. `S01-T04`
4. `S01-T06`
5. `S01-T05`
6. `S01-T02`
7. `S01-T07`
8. `S01-T08`
9. `S01-T09`
10. `S01-T10`
11. `S01-T11`
12. `S01-T12`
13. `S01-T13`
14. `S01-T14`
15. `S01-T15`
16. `S01-T16`
17. `S01-T18`
18. `S01-T19`

Execution rule for `S01-AUTO`: each subtask must be tested against its own acceptance criteria before moving to the next subtask. If acceptance criteria are still not met after 3 implementation-and-test attempts, record that explicitly in the subtask `Notes` and only then continue to the next subtask. The schema subtasks use dependency-aware ordering so foreign keys can be created cleanly.

| ID | Task | Purpose / Scope | Acceptance Criteria | Owner | Status | Notes |
|---|---|---|---|---|---|---|
| S01-T01 | Write migration: create `intake_items` table per Concept Update §11 (with indexes on status, captured_at, is_peter_kapitein) | Establish the intake queue source table and its core operational indexes. | Migration creates `intake_items` with Concept Update fields and the three named indexes; schema applies cleanly. | TBD | Completed | Implemented in `supabase/migrations/00028_comms_intake_items.sql`. Acceptance verified after making pre-Sprint migrations `00007` to `00013` compatible with a fresh local database: `pnpm dlx supabase@2.76.10 start` applied through `00035` in migration-only mode and `pnpm dlx supabase@2.76.10 db lint --local --fail-on error` returned `No schema errors found`. |
| S01-T02 | Write migration: create `content_calendar` table with FKs to `intake_items`, `initiatives`, `events` | Establish calendar storage and its relationships to upstream and related content records. | Migration creates `content_calendar` with the listed foreign keys and expected scheduling/status fields; FK references resolve cleanly. | TBD | Completed | Implemented in `supabase/migrations/00033_comms_content_calendar.sql` after `S01-T03`. Acceptance verified with the same local migration-only apply that reached `00035`, plus clean local schema lint. FK references now apply successfully in sequence. |
| S01-T03 | Write migration: create `events` table with lifecycle stages and output checklist columns | Provide the event pipeline data model needed for Sprint 03 routing and tracking. | Migration creates `events` with lifecycle-stage fields and output checklist columns described by the sprint; schema applies cleanly. | TBD | Completed | Implemented in `supabase/migrations/00029_comms_events.sql` before `S01-T02`. Acceptance verified by local Supabase migration apply through `00035` and `db lint --local --fail-on error` passing with no schema errors. |
| S01-T04 | Write migration: create `campus_sessions` table with hub/initiative/media references | Provide storage for World Campus session records and linked references. | Migration creates `campus_sessions` with session metadata plus hub, initiative, and media references; schema applies cleanly. | TBD | Completed | Implemented in `supabase/migrations/00030_comms_campus_sessions.sql`. The circular `slides_media_id` relation is intentionally finalized in `S01-T06`. Acceptance verified by local migration apply through `00035` plus clean schema lint. |
| S01-T05 | Write migration: create `campus_members` table with country, organisation, Peter-welcomed flag | Provide the member directory model used for World Campus relationship tracking. | Migration creates `campus_members` with identity, organisation, country, and Peter-related fields; schema applies cleanly. | TBD | Completed | Implemented in `supabase/migrations/00032_comms_campus_members.sql`. Acceptance verified by local migration apply through `00035` and `db lint --local --fail-on error` passing cleanly. |
| S01-T06 | Write migration: create `media_assets` table with SharePoint URL and rights status | Provide the Phase 1 media library data model. | Migration creates `media_assets` with SharePoint URL, rights status, and linking fields needed by the media module; schema applies cleanly. | TBD | Completed | Implemented in `supabase/migrations/00031_comms_media_assets.sql`, including the deferred `campus_sessions_slides_media_id_fkey` to close the circular relation from `S01-T04`. Acceptance verified by local migration apply through `00035` plus clean schema lint. |
| S01-T07 | Write migration: add `comms_team boolean default false` column to `profiles`; backfill existing rows | Introduce the access flag that unlocks the communications workspace. | Migration adds `profiles.comms_team` with default `false`, backfills existing rows safely, and leaves legacy users valid. | TBD | Completed | Implemented in `supabase/migrations/00034_profiles_comms_team.sql`, including `Moderator` reconciliation in `profiles_role_check`. Acceptance verified by local migration apply through `00035` and clean local schema lint. |
| S01-T08 | Write RLS policies for all six new tables (Moderator+`comms_team` read/write; Admin full; others denied) | Enforce the Sprint 01 permission model at the database layer. | Each new table has RLS enabled with admin full access, moderator plus `comms_team` read/write, and other roles denied by default. | TBD | Completed | Implemented in `supabase/migrations/00035_comms_permissions_rls.sql` with `public.is_comms_team_or_admin()` and one `FOR ALL` policy per communications table. Acceptance verified by local migration apply through `00035` and `db lint --local --fail-on error` returning `No schema errors found`. |
| S01-T09 | Regenerate `src/types/database.ts` and commit | Keep application types aligned with the new schema before feature work builds on it. | `src/types/database.ts` includes all Sprint 01 schema changes and compiles without ad hoc `any` workarounds. | TBD | Completed | `src/types/database.ts` now includes all six communications tables, `profiles.comms_team`, and the `is_comms_team_or_admin` function. Acceptance verified with `pnpm exec tsc --noEmit`. |
| S01-T10 | Create `/app/comms/layout.tsx` with sub-module nav (Intake / Calendar / Events / Campus Log / Media) | Create the shared shell for all communications routes. | `/app/comms` has a working layout with navigation entries for all five sub-modules and correct active-state handling. | TBD | Completed | Implemented in `src/app/app/comms/layout.tsx` and `src/app/app/comms/page.tsx` with tab navigation for all five modules. Acceptance verified via `pnpm exec next build`, which includes `/app/comms` and all child routes. |
| S01-T11 | Create placeholder pages for the five sub-routes (`/app/comms/intake`, `/calendar`, `/events`, `/campus-log`, `/media`) with empty-state copy | Give every planned communications route a navigable placeholder surface. | All five sub-routes render under the comms layout and show clear empty-state copy instead of 404s or blank pages. | TBD | Completed | Implemented with `src/components/comms/comms-placeholder.tsx` plus the five route pages under `src/app/app/comms/*`. Acceptance verified via `pnpm exec next build`, which generated all five routes successfully. |
| S01-T12 | Update post-login redirect: profiles with `comms_team = true` land on `/app/comms/intake` | Route comms-team users into their new default workspace after auth. | Users with `comms_team = true` are redirected to `/app/comms/intake` after login while non-comms behavior remains correct. | TBD | Completed | Implemented in `src/app/auth/callback/route.ts`, `middleware.ts`, and `src/lib/comms-access.ts`. Acceptance verified by `pnpm exec vitest run src/test/unit/middleware-routing.test.ts src/test/unit/role-access.test.ts src/test/unit/permissions.test.ts`. |
| S01-T13 | Surface "Communications" item in `SideNav` for `comms_team = true` users and Platform Admins | Make the comms module discoverable only for allowed users. | SideNav shows the Communications entry for comms-team users and Platform Admins, and hides it for unauthorized roles. | TBD | Completed | Implemented in `src/lib/role-access.ts`, `src/app/app/layout.tsx`, `src/components/layouts/side-nav.tsx`, and `src/components/layouts/top-nav.tsx`. Acceptance verified by navigation unit tests plus `pnpm exec next build`. |
| S01-T14 | Extend `docs/ROLE_PERMISSION_MODEL.md` with the Concept Update §9 permission matrix rows | Keep the documented permissions model aligned with the new comms module. | The role-permission doc includes the new communications rows and reflects Sprint 01 access rules accurately. | TBD | Completed | `docs/ROLE_PERMISSION_MODEL.md` was rewritten to match the canonical role model, add the `comms` matrix row, and document the `Moderator + comms_team` runtime rule. |
| S01-T15 | Add Atefeh demo persona to `supabase/seed-demo.sql` with `comms_team = true` | Ensure demo and local verification can use a realistic comms-team persona. | Seed data includes Atefeh with `comms_team = true` and enough profile data to exercise comms routing. | TBD | Completed | Added `atefeh@inspire2live.org` as a `Moderator` demo account in `supabase/seed-demo.sql` with auth user, identity, profile data, and `comms_team = true`. |
| S01-T16 | Unit tests: middleware routing for `comms_team` flag | Add regression coverage for the new access-routing behavior. | Middleware tests cover comms-team redirect behavior and protect existing non-comms routing paths from regression. | TBD | Completed | Expanded `src/test/unit/middleware-routing.test.ts` and related nav/permissions tests. Acceptance verified by `pnpm exec vitest run src/test/unit/middleware-routing.test.ts src/test/unit/role-access.test.ts src/test/unit/permissions.test.ts` with 56 passing tests. |
| S01-T17 | Apply for WhatsApp Business API account via Meta Business Manager | Start the external dependency with the longest lead time in the MVP plan. | The application is submitted or the blocker is recorded in sprint notes with next step and owner. | TBD | Not Started | Non-engineering; long lead time |
| S01-T18 | Update `docs/TRACEABILITY.md` with new REQ-COMMS-001 through REQ-COMMS-00X entries | Restore traceability for the new communications scope introduced by the pivot. | Traceability doc contains Sprint 01 REQ rows mapped to planned code locations and statuses. | TBD | Completed | Added `REQ-COMMS-001` through `REQ-COMMS-009` to `docs/TRACEABILITY.md`, including explicit `blocked` status for schema items that could not be runtime-applied locally. |
| S01-T19 | Author ADR documenting the Communications Workspace module decision and the six new tables | Capture the architectural decision behind the new module and schema footprint. | A new ADR explains the module decision, the six new tables, and the relationship to the MVP pivot. | TBD | Completed | Authored `docs/ADR/0006-communications-workspace.md` covering module scope, access model, six-table schema footprint, and rollout rationale. |

## Execution Bundles

| Bundle | Task IDs | Delivery description | Recommended model | Rationale | Intermediate QC | Human QC boundary |
|---|---|---|---|---|---|---|
| S01-B01 | S01-T01 to S01-T06 | Ship the six Communications Workspace tables as one coherent schema slice so downstream sprint work has a stable foundation. | GPT-5 high | Cross-table schema design, foreign keys, and migration coupling are high-risk to get wrong piecemeal. | Validate FK graph, indexes, defaults, and migration ordering; verify table names match Concept Update and future route needs. | Autonomous. Part of parent stream `S01-AUTO`; each subtask gets up to 3 implementation-and-test attempts before an explicit blocker note is required. |
| S01-B02 | S01-T07 to S01-T09 | Finish the access layer for Sprint 01 by adding `comms_team`, locking down RLS, and regenerating DB types in the same pass. | GPT-5 high | Auth, redirects, and RLS have hidden coupling and broad regression risk. | Verify role matrix, backfill behavior, RLS deny-by-default posture, and generated types compile cleanly. | Autonomous. Part of parent stream `S01-AUTO`; each subtask gets up to 3 implementation-and-test attempts before an explicit blocker note is required. |
| S01-B03 | S01-T10 to S01-T11 | Stand up the `/app/comms` shell with all five placeholder routes so later sprints build on a real navigation frame. | GPT-5 medium | Multi-file UI work is straightforward but touches shared app-shell patterns. | Render each route, confirm nav states and empty states, and check desktop/mobile layout sanity. | Autonomous. Part of parent stream `S01-AUTO`; each subtask gets up to 3 implementation-and-test attempts before an explicit blocker note is required. |
| S01-B04 | S01-T12 to S01-T13 | Route comms-team users into the new module and surface the Communications nav entry only where allowed. | GPT-5 high | Login redirect and role-aware navigation are permission-sensitive and easy to regress. | Check redirect logic, denied-user behavior, nav visibility rules, and middleware/auth callback consistency. | Autonomous. Part of parent stream `S01-AUTO`; each subtask gets up to 3 implementation-and-test attempts before an explicit blocker note is required. |
| S01-B05 | S01-T14, S01-T18, S01-T19 | Update the governance layer so permissions, traceability, and the architecture decision are documented with the shipped foundation. | GPT-4 medium | This is mostly structured documentation tied to known implementation details. | Cross-check REQ IDs, permission language, ADR scope, and doc links against the implemented schema and routing behavior. | Autonomous unless policy wording needs leadership approval. Part of parent stream `S01-AUTO`; each subtask gets up to 3 implementation-and-test attempts before an explicit blocker note is required. |
| S01-B06 | S01-T15 to S01-T16 | Add the demo persona and regression coverage so Sprint 01 is testable with realistic comms-team access. | GPT-5 medium | Seed and test work is technical, coupled to new access behavior, and benefits from stronger reasoning. | Verify seed inserts valid fields, middleware tests cover comms-team redirects, and test names reflect the new behavior. | Autonomous. Part of parent stream `S01-AUTO`; each subtask gets up to 3 implementation-and-test attempts before an explicit blocker note is required. |
| S01-B07 | S01-T17 | Submit the WhatsApp Business API application and record the outcome in the sprint notes. | GPT-4 low | This is an external operational checklist, not a reasoning-heavy engineering task. | Confirm account prerequisites, submission captured, and follow-up date recorded. | Human action required because this touches external business accounts. |
