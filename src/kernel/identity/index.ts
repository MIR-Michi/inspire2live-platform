/**
 * kernel/identity — auth + platform identity primitives.
 *
 * The canonical *contact* spine (ADR-0007) lives in the `contacts` component,
 * not here; kernel identity is the thin auth layer everything depends on.
 */
export * from '@/kernel/identity/auth-redirect-url'
